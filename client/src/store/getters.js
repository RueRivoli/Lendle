const getters = {
    GET_AUTH: (state) => state.authentificated,
    GET_LOAN: (state) => state.toLoan,
    GET_TOKEN: (state) => state.token,
    GET_ID: (state) => state.id
  }
  
  export default getters

